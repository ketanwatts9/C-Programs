// A Structure in C is a user - defined data type that allows us to combine different types of data under a single name.It is used to represent a record containing related information, such as a student's roll number, name, and marks. Structures are declared using the struct keyword. Each variable inside a structure is called a member. Structure members can be accessed using the dot (.) operator. struct Student
// {
//     int rollNo;
//     char name[50];
//     float marks;
// };